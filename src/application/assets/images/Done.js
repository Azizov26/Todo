import React from "react";

import done from '../images/done.png'

export const Done = () => {
	return (
		<img style={{
			width: '20px',
			height:'20px',
			display: 'inline-block',
			position: 'relative',

		}} src={done} alt='edit'/>
	)
}